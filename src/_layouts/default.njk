<!doctype html>
<html lang="{{ page.lang }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <meta name="description" content="{{ page.filePathStem }}"/>
  </head>

  <body>
    {% include "header.njk" %}

    <hr>

    <main>
      {{ content | safe }}
    </main>

    <hr>
      
    {% include "footer.njk" %}
  </body>
</html>
